services:
  - type: web
    name: face-recognition-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars: []
    autoDeploy: true
    branch: main
